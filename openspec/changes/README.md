# Changes (activos)

Cada carpeta bajo `changes/` representa un cambio propuesto (feature, refactor, spike).

Generar con:

```bash
# En shell
rtk proxy openspec new <nombre>
```

No implementar código de producción sin spec asociada en el change.
