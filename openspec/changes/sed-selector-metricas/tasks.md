# Tasks: SED Selector y Métricas

- [ ] `GlobalGoalCreateForm.svelte`: reemplazar buscador por selector DaisyUI paged + nested con breadcrumb
- [ ] Endpoint/params de árbol: orden `ltree path`, paginación cursor, búsqueda acotada a rama
- [ ] `metrics_service.go`: filtrar por `phase` (default `current_phase`), separar equipo + jefe
- [ ] OpenAPI: documentar `phase` en métricas y orden/paginación del árbol
- [ ] Tests: selector pagina ramas; métricas avance ≠ cierre; `openspec validate --all`
