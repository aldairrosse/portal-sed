services:
  postgres:
    image: postgres:16-alpine
    ports: ["127.0.0.1:5432:5432"]
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sed"]
      interval: 5s
      retries: 5

  api:
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    working_dir: /app
    volumes:
      - ./api:/app
      - go-mod:/go/pkg/mod
      - go-build:/root/.cache/go-build
    depends_on:
      postgres:
        condition: service_healthy
    env_file: .env
    environment:
      ENV: development
      MAIL_PROVIDER: ${MAIL_PROVIDER:-no-op}
      SENDGRID_API_KEY: ${SENDGRID_API_KEY:-}
      MAIL_FROM: ${MAIL_FROM:-no-reply@sed.local}
      APP_BASE_URL: ${APP_BASE_URL:-http://localhost:5173}
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  import:
    build: ./api
    profiles: ["import"]
    network_mode: host
    depends_on:
      postgres:
        condition: service_healthy
    env_file: .env
    entrypoint: ["/opt/api/import"]

  web:
    build:
      context: ./web
      dockerfile: Dockerfile.dev
    working_dir: /app
    volumes:
      - ./web:/app
      - web-node-modules:/app/node_modules
    depends_on:
      - api

  proxy:
    image: nginx:stable-alpine
    ports:
      - "8088:80"
    volumes:
      - ./nginx.dev.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web
      - api

volumes:
  pgdata:
  go-mod:
  go-build:
  web-node-modules:
