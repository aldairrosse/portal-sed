// Code generated by ent, DO NOT EDIT.

package internal

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/sed-evaluacion-desempeno/api/internal/cycle"
	"github.com/sed-evaluacion-desempeno/api/internal/evaluation"
	"github.com/sed-evaluacion-desempeno/api/internal/evaluatorscope"
	"github.com/sed-evaluacion-desempeno/api/internal/goalassignment"
	"github.com/sed-evaluacion-desempeno/api/internal/nineboxmatrix"
	"github.com/sed-evaluacion-desempeno/api/internal/organization"
	"github.com/sed-evaluacion-desempeno/api/internal/phasedefinition"
	"github.com/sed-evaluacion-desempeno/api/internal/phasetransition"
	"github.com/sed-evaluacion-desempeno/api/internal/predicate"
)

// CycleQuery is the builder for querying Cycle entities.
type CycleQuery struct {
	config
	ctx                  *QueryContext
	order                []cycle.OrderOption
	inters               []Interceptor
	predicates           []predicate.Cycle
	withOrganization     *OrganizationQuery
	withPhaseTransitions *PhaseTransitionQuery
	withPhaseDefinitions *PhaseDefinitionQuery
	withEvaluatorScopes  *EvaluatorScopeQuery
	withGoalAssignments  *GoalAssignmentQuery
	withEvaluations      *EvaluationQuery
	withNineBoxMatrices  *NineBoxMatrixQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CycleQuery builder.
func (_q *CycleQuery) Where(ps ...predicate.Cycle) *CycleQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *CycleQuery) Limit(limit int) *CycleQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *CycleQuery) Offset(offset int) *CycleQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *CycleQuery) Unique(unique bool) *CycleQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *CycleQuery) Order(o ...cycle.OrderOption) *CycleQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *CycleQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cycle.Table, cycle.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, cycle.OrganizationTable, cycle.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPhaseTransitions chains the current query on the "phase_transitions" edge.
func (_q *CycleQuery) QueryPhaseTransitions() *PhaseTransitionQuery {
	query := (&PhaseTransitionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cycle.Table, cycle.FieldID, selector),
			sqlgraph.To(phasetransition.Table, phasetransition.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, cycle.PhaseTransitionsTable, cycle.PhaseTransitionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPhaseDefinitions chains the current query on the "phase_definitions" edge.
func (_q *CycleQuery) QueryPhaseDefinitions() *PhaseDefinitionQuery {
	query := (&PhaseDefinitionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cycle.Table, cycle.FieldID, selector),
			sqlgraph.To(phasedefinition.Table, phasedefinition.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, cycle.PhaseDefinitionsTable, cycle.PhaseDefinitionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEvaluatorScopes chains the current query on the "evaluator_scopes" edge.
func (_q *CycleQuery) QueryEvaluatorScopes() *EvaluatorScopeQuery {
	query := (&EvaluatorScopeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cycle.Table, cycle.FieldID, selector),
			sqlgraph.To(evaluatorscope.Table, evaluatorscope.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, cycle.EvaluatorScopesTable, cycle.EvaluatorScopesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGoalAssignments chains the current query on the "goal_assignments" edge.
func (_q *CycleQuery) QueryGoalAssignments() *GoalAssignmentQuery {
	query := (&GoalAssignmentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cycle.Table, cycle.FieldID, selector),
			sqlgraph.To(goalassignment.Table, goalassignment.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, cycle.GoalAssignmentsTable, cycle.GoalAssignmentsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEvaluations chains the current query on the "evaluations" edge.
func (_q *CycleQuery) QueryEvaluations() *EvaluationQuery {
	query := (&EvaluationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cycle.Table, cycle.FieldID, selector),
			sqlgraph.To(evaluation.Table, evaluation.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, cycle.EvaluationsTable, cycle.EvaluationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNineBoxMatrices chains the current query on the "nine_box_matrices" edge.
func (_q *CycleQuery) QueryNineBoxMatrices() *NineBoxMatrixQuery {
	query := (&NineBoxMatrixClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cycle.Table, cycle.FieldID, selector),
			sqlgraph.To(nineboxmatrix.Table, nineboxmatrix.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, cycle.NineBoxMatricesTable, cycle.NineBoxMatricesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Cycle entity from the query.
// Returns a *NotFoundError when no Cycle was found.
func (_q *CycleQuery) First(ctx context.Context) (*Cycle, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cycle.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *CycleQuery) FirstX(ctx context.Context) *Cycle {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Cycle ID from the query.
// Returns a *NotFoundError when no Cycle ID was found.
func (_q *CycleQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cycle.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *CycleQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Cycle entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Cycle entity is found.
// Returns a *NotFoundError when no Cycle entities are found.
func (_q *CycleQuery) Only(ctx context.Context) (*Cycle, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cycle.Label}
	default:
		return nil, &NotSingularError{cycle.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *CycleQuery) OnlyX(ctx context.Context) *Cycle {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Cycle ID in the query.
// Returns a *NotSingularError when more than one Cycle ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *CycleQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cycle.Label}
	default:
		err = &NotSingularError{cycle.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *CycleQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Cycles.
func (_q *CycleQuery) All(ctx context.Context) ([]*Cycle, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Cycle, *CycleQuery]()
	return withInterceptors[[]*Cycle](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *CycleQuery) AllX(ctx context.Context) []*Cycle {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Cycle IDs.
func (_q *CycleQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(cycle.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *CycleQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *CycleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*CycleQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *CycleQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *CycleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("internal: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *CycleQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CycleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *CycleQuery) Clone() *CycleQuery {
	if _q == nil {
		return nil
	}
	return &CycleQuery{
		config:               _q.config,
		ctx:                  _q.ctx.Clone(),
		order:                append([]cycle.OrderOption{}, _q.order...),
		inters:               append([]Interceptor{}, _q.inters...),
		predicates:           append([]predicate.Cycle{}, _q.predicates...),
		withOrganization:     _q.withOrganization.Clone(),
		withPhaseTransitions: _q.withPhaseTransitions.Clone(),
		withPhaseDefinitions: _q.withPhaseDefinitions.Clone(),
		withEvaluatorScopes:  _q.withEvaluatorScopes.Clone(),
		withGoalAssignments:  _q.withGoalAssignments.Clone(),
		withEvaluations:      _q.withEvaluations.Clone(),
		withNineBoxMatrices:  _q.withNineBoxMatrices.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CycleQuery) WithOrganization(opts ...func(*OrganizationQuery)) *CycleQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// WithPhaseTransitions tells the query-builder to eager-load the nodes that are connected to
// the "phase_transitions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CycleQuery) WithPhaseTransitions(opts ...func(*PhaseTransitionQuery)) *CycleQuery {
	query := (&PhaseTransitionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPhaseTransitions = query
	return _q
}

// WithPhaseDefinitions tells the query-builder to eager-load the nodes that are connected to
// the "phase_definitions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CycleQuery) WithPhaseDefinitions(opts ...func(*PhaseDefinitionQuery)) *CycleQuery {
	query := (&PhaseDefinitionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPhaseDefinitions = query
	return _q
}

// WithEvaluatorScopes tells the query-builder to eager-load the nodes that are connected to
// the "evaluator_scopes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CycleQuery) WithEvaluatorScopes(opts ...func(*EvaluatorScopeQuery)) *CycleQuery {
	query := (&EvaluatorScopeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEvaluatorScopes = query
	return _q
}

// WithGoalAssignments tells the query-builder to eager-load the nodes that are connected to
// the "goal_assignments" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CycleQuery) WithGoalAssignments(opts ...func(*GoalAssignmentQuery)) *CycleQuery {
	query := (&GoalAssignmentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGoalAssignments = query
	return _q
}

// WithEvaluations tells the query-builder to eager-load the nodes that are connected to
// the "evaluations" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CycleQuery) WithEvaluations(opts ...func(*EvaluationQuery)) *CycleQuery {
	query := (&EvaluationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEvaluations = query
	return _q
}

// WithNineBoxMatrices tells the query-builder to eager-load the nodes that are connected to
// the "nine_box_matrices" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CycleQuery) WithNineBoxMatrices(opts ...func(*NineBoxMatrixQuery)) *CycleQuery {
	query := (&NineBoxMatrixClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNineBoxMatrices = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Cycle.Query().
//		GroupBy(cycle.FieldCreatedAt).
//		Aggregate(internal.Count()).
//		Scan(ctx, &v)
func (_q *CycleQuery) GroupBy(field string, fields ...string) *CycleGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CycleGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = cycle.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Cycle.Query().
//		Select(cycle.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *CycleQuery) Select(fields ...string) *CycleSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &CycleSelect{CycleQuery: _q}
	sbuild.label = cycle.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CycleSelect configured with the given aggregations.
func (_q *CycleQuery) Aggregate(fns ...AggregateFunc) *CycleSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *CycleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("internal: uninitialized interceptor (forgotten import internal/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !cycle.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("internal: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *CycleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Cycle, error) {
	var (
		nodes       = []*Cycle{}
		_spec       = _q.querySpec()
		loadedTypes = [7]bool{
			_q.withOrganization != nil,
			_q.withPhaseTransitions != nil,
			_q.withPhaseDefinitions != nil,
			_q.withEvaluatorScopes != nil,
			_q.withGoalAssignments != nil,
			_q.withEvaluations != nil,
			_q.withNineBoxMatrices != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Cycle).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Cycle{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *Cycle, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPhaseTransitions; query != nil {
		if err := _q.loadPhaseTransitions(ctx, query, nodes,
			func(n *Cycle) { n.Edges.PhaseTransitions = []*PhaseTransition{} },
			func(n *Cycle, e *PhaseTransition) { n.Edges.PhaseTransitions = append(n.Edges.PhaseTransitions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPhaseDefinitions; query != nil {
		if err := _q.loadPhaseDefinitions(ctx, query, nodes,
			func(n *Cycle) { n.Edges.PhaseDefinitions = []*PhaseDefinition{} },
			func(n *Cycle, e *PhaseDefinition) { n.Edges.PhaseDefinitions = append(n.Edges.PhaseDefinitions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEvaluatorScopes; query != nil {
		if err := _q.loadEvaluatorScopes(ctx, query, nodes,
			func(n *Cycle) { n.Edges.EvaluatorScopes = []*EvaluatorScope{} },
			func(n *Cycle, e *EvaluatorScope) { n.Edges.EvaluatorScopes = append(n.Edges.EvaluatorScopes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withGoalAssignments; query != nil {
		if err := _q.loadGoalAssignments(ctx, query, nodes,
			func(n *Cycle) { n.Edges.GoalAssignments = []*GoalAssignment{} },
			func(n *Cycle, e *GoalAssignment) { n.Edges.GoalAssignments = append(n.Edges.GoalAssignments, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEvaluations; query != nil {
		if err := _q.loadEvaluations(ctx, query, nodes,
			func(n *Cycle) { n.Edges.Evaluations = []*Evaluation{} },
			func(n *Cycle, e *Evaluation) { n.Edges.Evaluations = append(n.Edges.Evaluations, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withNineBoxMatrices; query != nil {
		if err := _q.loadNineBoxMatrices(ctx, query, nodes,
			func(n *Cycle) { n.Edges.NineBoxMatrices = []*NineBoxMatrix{} },
			func(n *Cycle, e *NineBoxMatrix) { n.Edges.NineBoxMatrices = append(n.Edges.NineBoxMatrices, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *CycleQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*Cycle, init func(*Cycle), assign func(*Cycle, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Cycle)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CycleQuery) loadPhaseTransitions(ctx context.Context, query *PhaseTransitionQuery, nodes []*Cycle, init func(*Cycle), assign func(*Cycle, *PhaseTransition)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Cycle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(phasetransition.FieldCycleID)
	}
	query.Where(predicate.PhaseTransition(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(cycle.PhaseTransitionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CycleID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cycle_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CycleQuery) loadPhaseDefinitions(ctx context.Context, query *PhaseDefinitionQuery, nodes []*Cycle, init func(*Cycle), assign func(*Cycle, *PhaseDefinition)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Cycle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(phasedefinition.FieldCycleID)
	}
	query.Where(predicate.PhaseDefinition(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(cycle.PhaseDefinitionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CycleID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cycle_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CycleQuery) loadEvaluatorScopes(ctx context.Context, query *EvaluatorScopeQuery, nodes []*Cycle, init func(*Cycle), assign func(*Cycle, *EvaluatorScope)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Cycle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(evaluatorscope.FieldCycleID)
	}
	query.Where(predicate.EvaluatorScope(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(cycle.EvaluatorScopesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CycleID
		if fk == nil {
			return fmt.Errorf(`foreign-key "cycle_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cycle_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CycleQuery) loadGoalAssignments(ctx context.Context, query *GoalAssignmentQuery, nodes []*Cycle, init func(*Cycle), assign func(*Cycle, *GoalAssignment)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Cycle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(goalassignment.FieldCycleID)
	}
	query.Where(predicate.GoalAssignment(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(cycle.GoalAssignmentsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CycleID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cycle_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CycleQuery) loadEvaluations(ctx context.Context, query *EvaluationQuery, nodes []*Cycle, init func(*Cycle), assign func(*Cycle, *Evaluation)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Cycle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(evaluation.FieldCycleID)
	}
	query.Where(predicate.Evaluation(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(cycle.EvaluationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CycleID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cycle_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CycleQuery) loadNineBoxMatrices(ctx context.Context, query *NineBoxMatrixQuery, nodes []*Cycle, init func(*Cycle), assign func(*Cycle, *NineBoxMatrix)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Cycle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(nineboxmatrix.FieldCycleID)
	}
	query.Where(predicate.NineBoxMatrix(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(cycle.NineBoxMatricesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CycleID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cycle_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *CycleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *CycleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(cycle.Table, cycle.Columns, sqlgraph.NewFieldSpec(cycle.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cycle.FieldID)
		for i := range fields {
			if fields[i] != cycle.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(cycle.FieldOrganizationID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *CycleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(cycle.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = cycle.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CycleGroupBy is the group-by builder for Cycle entities.
type CycleGroupBy struct {
	selector
	build *CycleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *CycleGroupBy) Aggregate(fns ...AggregateFunc) *CycleGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *CycleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CycleQuery, *CycleGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *CycleGroupBy) sqlScan(ctx context.Context, root *CycleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CycleSelect is the builder for selecting fields of Cycle entities.
type CycleSelect struct {
	*CycleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *CycleSelect) Aggregate(fns ...AggregateFunc) *CycleSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *CycleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CycleQuery, *CycleSelect](ctx, _s.CycleQuery, _s, _s.inters, v)
}

func (_s *CycleSelect) sqlScan(ctx context.Context, root *CycleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
