// Code generated by ent, DO NOT EDIT.

package internal

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/sed-evaluacion-desempeno/api/internal/pillar"
)

// Pillar is the model entity for the Pillar schema.
type Pillar struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Version holds the value of the "version" field.
	Version int `json:"version,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PillarQuery when eager-loading is set.
	Edges        PillarEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PillarEdges holds the relations/edges for other nodes in the graph.
type PillarEdges struct {
	// Competencies holds the value of the competencies edge.
	Competencies []*Competency `json:"competencies,omitempty"`
	// ScaleCriteria holds the value of the scale_criteria edge.
	ScaleCriteria []*ScaleCriterion `json:"scale_criteria,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CompetenciesOrErr returns the Competencies value or an error if the edge
// was not loaded in eager-loading.
func (e PillarEdges) CompetenciesOrErr() ([]*Competency, error) {
	if e.loadedTypes[0] {
		return e.Competencies, nil
	}
	return nil, &NotLoadedError{edge: "competencies"}
}

// ScaleCriteriaOrErr returns the ScaleCriteria value or an error if the edge
// was not loaded in eager-loading.
func (e PillarEdges) ScaleCriteriaOrErr() ([]*ScaleCriterion, error) {
	if e.loadedTypes[1] {
		return e.ScaleCriteria, nil
	}
	return nil, &NotLoadedError{edge: "scale_criteria"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Pillar) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pillar.FieldVersion:
			values[i] = new(sql.NullInt64)
		case pillar.FieldName, pillar.FieldDescription:
			values[i] = new(sql.NullString)
		case pillar.FieldCreatedAt, pillar.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case pillar.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Pillar fields.
func (_m *Pillar) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pillar.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case pillar.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case pillar.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case pillar.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				_m.Version = int(value.Int64)
			}
		case pillar.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case pillar.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Pillar.
// This includes values selected through modifiers, order, etc.
func (_m *Pillar) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCompetencies queries the "competencies" edge of the Pillar entity.
func (_m *Pillar) QueryCompetencies() *CompetencyQuery {
	return NewPillarClient(_m.config).QueryCompetencies(_m)
}

// QueryScaleCriteria queries the "scale_criteria" edge of the Pillar entity.
func (_m *Pillar) QueryScaleCriteria() *ScaleCriterionQuery {
	return NewPillarClient(_m.config).QueryScaleCriteria(_m)
}

// Update returns a builder for updating this Pillar.
// Note that you need to call Pillar.Unwrap() before calling this method if this Pillar
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Pillar) Update() *PillarUpdateOne {
	return NewPillarClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Pillar entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Pillar) Unwrap() *Pillar {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("internal: Pillar is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Pillar) String() string {
	var builder strings.Builder
	builder.WriteString("Pillar(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", _m.Version))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteByte(')')
	return builder.String()
}

// Pillars is a parsable slice of Pillar.
type Pillars []*Pillar
