// Code generated by ent, DO NOT EDIT.

package internal

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/sed-evaluacion-desempeno/api/internal/goaltemplate"
	"github.com/sed-evaluacion-desempeno/api/internal/goaltemplatekpilink"
	"github.com/sed-evaluacion-desempeno/api/internal/kpi"
	"github.com/sed-evaluacion-desempeno/api/internal/predicate"
)

// GoalTemplateKpiLinkQuery is the builder for querying GoalTemplateKpiLink entities.
type GoalTemplateKpiLinkQuery struct {
	config
	ctx          *QueryContext
	order        []goaltemplatekpilink.OrderOption
	inters       []Interceptor
	predicates   []predicate.GoalTemplateKpiLink
	withTemplate *GoalTemplateQuery
	withKpi      *KPIQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoalTemplateKpiLinkQuery builder.
func (_q *GoalTemplateKpiLinkQuery) Where(ps ...predicate.GoalTemplateKpiLink) *GoalTemplateKpiLinkQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *GoalTemplateKpiLinkQuery) Limit(limit int) *GoalTemplateKpiLinkQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *GoalTemplateKpiLinkQuery) Offset(offset int) *GoalTemplateKpiLinkQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *GoalTemplateKpiLinkQuery) Unique(unique bool) *GoalTemplateKpiLinkQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *GoalTemplateKpiLinkQuery) Order(o ...goaltemplatekpilink.OrderOption) *GoalTemplateKpiLinkQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryTemplate chains the current query on the "template" edge.
func (_q *GoalTemplateKpiLinkQuery) QueryTemplate() *GoalTemplateQuery {
	query := (&GoalTemplateClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(goaltemplatekpilink.Table, goaltemplatekpilink.FieldID, selector),
			sqlgraph.To(goaltemplate.Table, goaltemplate.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, goaltemplatekpilink.TemplateTable, goaltemplatekpilink.TemplateColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryKpi chains the current query on the "kpi" edge.
func (_q *GoalTemplateKpiLinkQuery) QueryKpi() *KPIQuery {
	query := (&KPIClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(goaltemplatekpilink.Table, goaltemplatekpilink.FieldID, selector),
			sqlgraph.To(kpi.Table, kpi.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, goaltemplatekpilink.KpiTable, goaltemplatekpilink.KpiColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first GoalTemplateKpiLink entity from the query.
// Returns a *NotFoundError when no GoalTemplateKpiLink was found.
func (_q *GoalTemplateKpiLinkQuery) First(ctx context.Context) (*GoalTemplateKpiLink, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goaltemplatekpilink.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *GoalTemplateKpiLinkQuery) FirstX(ctx context.Context) *GoalTemplateKpiLink {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoalTemplateKpiLink ID from the query.
// Returns a *NotFoundError when no GoalTemplateKpiLink ID was found.
func (_q *GoalTemplateKpiLinkQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goaltemplatekpilink.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *GoalTemplateKpiLinkQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoalTemplateKpiLink entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoalTemplateKpiLink entity is found.
// Returns a *NotFoundError when no GoalTemplateKpiLink entities are found.
func (_q *GoalTemplateKpiLinkQuery) Only(ctx context.Context) (*GoalTemplateKpiLink, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goaltemplatekpilink.Label}
	default:
		return nil, &NotSingularError{goaltemplatekpilink.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *GoalTemplateKpiLinkQuery) OnlyX(ctx context.Context) *GoalTemplateKpiLink {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoalTemplateKpiLink ID in the query.
// Returns a *NotSingularError when more than one GoalTemplateKpiLink ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *GoalTemplateKpiLinkQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goaltemplatekpilink.Label}
	default:
		err = &NotSingularError{goaltemplatekpilink.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *GoalTemplateKpiLinkQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoalTemplateKpiLinks.
func (_q *GoalTemplateKpiLinkQuery) All(ctx context.Context) ([]*GoalTemplateKpiLink, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GoalTemplateKpiLink, *GoalTemplateKpiLinkQuery]()
	return withInterceptors[[]*GoalTemplateKpiLink](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *GoalTemplateKpiLinkQuery) AllX(ctx context.Context) []*GoalTemplateKpiLink {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoalTemplateKpiLink IDs.
func (_q *GoalTemplateKpiLinkQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(goaltemplatekpilink.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *GoalTemplateKpiLinkQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *GoalTemplateKpiLinkQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*GoalTemplateKpiLinkQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *GoalTemplateKpiLinkQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *GoalTemplateKpiLinkQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("internal: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *GoalTemplateKpiLinkQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoalTemplateKpiLinkQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *GoalTemplateKpiLinkQuery) Clone() *GoalTemplateKpiLinkQuery {
	if _q == nil {
		return nil
	}
	return &GoalTemplateKpiLinkQuery{
		config:       _q.config,
		ctx:          _q.ctx.Clone(),
		order:        append([]goaltemplatekpilink.OrderOption{}, _q.order...),
		inters:       append([]Interceptor{}, _q.inters...),
		predicates:   append([]predicate.GoalTemplateKpiLink{}, _q.predicates...),
		withTemplate: _q.withTemplate.Clone(),
		withKpi:      _q.withKpi.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithTemplate tells the query-builder to eager-load the nodes that are connected to
// the "template" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *GoalTemplateKpiLinkQuery) WithTemplate(opts ...func(*GoalTemplateQuery)) *GoalTemplateKpiLinkQuery {
	query := (&GoalTemplateClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTemplate = query
	return _q
}

// WithKpi tells the query-builder to eager-load the nodes that are connected to
// the "kpi" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *GoalTemplateKpiLinkQuery) WithKpi(opts ...func(*KPIQuery)) *GoalTemplateKpiLinkQuery {
	query := (&KPIClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withKpi = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoalTemplateKpiLink.Query().
//		GroupBy(goaltemplatekpilink.FieldCreatedAt).
//		Aggregate(internal.Count()).
//		Scan(ctx, &v)
func (_q *GoalTemplateKpiLinkQuery) GroupBy(field string, fields ...string) *GoalTemplateKpiLinkGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GoalTemplateKpiLinkGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = goaltemplatekpilink.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.GoalTemplateKpiLink.Query().
//		Select(goaltemplatekpilink.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *GoalTemplateKpiLinkQuery) Select(fields ...string) *GoalTemplateKpiLinkSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &GoalTemplateKpiLinkSelect{GoalTemplateKpiLinkQuery: _q}
	sbuild.label = goaltemplatekpilink.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GoalTemplateKpiLinkSelect configured with the given aggregations.
func (_q *GoalTemplateKpiLinkQuery) Aggregate(fns ...AggregateFunc) *GoalTemplateKpiLinkSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *GoalTemplateKpiLinkQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("internal: uninitialized interceptor (forgotten import internal/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !goaltemplatekpilink.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("internal: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *GoalTemplateKpiLinkQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoalTemplateKpiLink, error) {
	var (
		nodes       = []*GoalTemplateKpiLink{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withTemplate != nil,
			_q.withKpi != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GoalTemplateKpiLink).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GoalTemplateKpiLink{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withTemplate; query != nil {
		if err := _q.loadTemplate(ctx, query, nodes, nil,
			func(n *GoalTemplateKpiLink, e *GoalTemplate) { n.Edges.Template = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withKpi; query != nil {
		if err := _q.loadKpi(ctx, query, nodes, nil,
			func(n *GoalTemplateKpiLink, e *KPI) { n.Edges.Kpi = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *GoalTemplateKpiLinkQuery) loadTemplate(ctx context.Context, query *GoalTemplateQuery, nodes []*GoalTemplateKpiLink, init func(*GoalTemplateKpiLink), assign func(*GoalTemplateKpiLink, *GoalTemplate)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*GoalTemplateKpiLink)
	for i := range nodes {
		fk := nodes[i].TemplateID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(goaltemplate.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "template_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *GoalTemplateKpiLinkQuery) loadKpi(ctx context.Context, query *KPIQuery, nodes []*GoalTemplateKpiLink, init func(*GoalTemplateKpiLink), assign func(*GoalTemplateKpiLink, *KPI)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*GoalTemplateKpiLink)
	for i := range nodes {
		fk := nodes[i].KpiID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(kpi.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "kpi_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *GoalTemplateKpiLinkQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *GoalTemplateKpiLinkQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(goaltemplatekpilink.Table, goaltemplatekpilink.Columns, sqlgraph.NewFieldSpec(goaltemplatekpilink.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goaltemplatekpilink.FieldID)
		for i := range fields {
			if fields[i] != goaltemplatekpilink.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withTemplate != nil {
			_spec.Node.AddColumnOnce(goaltemplatekpilink.FieldTemplateID)
		}
		if _q.withKpi != nil {
			_spec.Node.AddColumnOnce(goaltemplatekpilink.FieldKpiID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *GoalTemplateKpiLinkQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(goaltemplatekpilink.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = goaltemplatekpilink.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GoalTemplateKpiLinkGroupBy is the group-by builder for GoalTemplateKpiLink entities.
type GoalTemplateKpiLinkGroupBy struct {
	selector
	build *GoalTemplateKpiLinkQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *GoalTemplateKpiLinkGroupBy) Aggregate(fns ...AggregateFunc) *GoalTemplateKpiLinkGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *GoalTemplateKpiLinkGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoalTemplateKpiLinkQuery, *GoalTemplateKpiLinkGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *GoalTemplateKpiLinkGroupBy) sqlScan(ctx context.Context, root *GoalTemplateKpiLinkQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GoalTemplateKpiLinkSelect is the builder for selecting fields of GoalTemplateKpiLink entities.
type GoalTemplateKpiLinkSelect struct {
	*GoalTemplateKpiLinkQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *GoalTemplateKpiLinkSelect) Aggregate(fns ...AggregateFunc) *GoalTemplateKpiLinkSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *GoalTemplateKpiLinkSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoalTemplateKpiLinkQuery, *GoalTemplateKpiLinkSelect](ctx, _s.GoalTemplateKpiLinkQuery, _s, _s.inters, v)
}

func (_s *GoalTemplateKpiLinkSelect) sqlScan(ctx context.Context, root *GoalTemplateKpiLinkQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
