// Code generated by ent, DO NOT EDIT.

package internal

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/sed-evaluacion-desempeno/api/internal/competency"
	"github.com/sed-evaluacion-desempeno/api/internal/pillar"
	"github.com/sed-evaluacion-desempeno/api/internal/predicate"
	"github.com/sed-evaluacion-desempeno/api/internal/scalecriterion"
)

// PillarQuery is the builder for querying Pillar entities.
type PillarQuery struct {
	config
	ctx               *QueryContext
	order             []pillar.OrderOption
	inters            []Interceptor
	predicates        []predicate.Pillar
	withCompetencies  *CompetencyQuery
	withScaleCriteria *ScaleCriterionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PillarQuery builder.
func (_q *PillarQuery) Where(ps ...predicate.Pillar) *PillarQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PillarQuery) Limit(limit int) *PillarQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PillarQuery) Offset(offset int) *PillarQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PillarQuery) Unique(unique bool) *PillarQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PillarQuery) Order(o ...pillar.OrderOption) *PillarQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCompetencies chains the current query on the "competencies" edge.
func (_q *PillarQuery) QueryCompetencies() *CompetencyQuery {
	query := (&CompetencyClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pillar.Table, pillar.FieldID, selector),
			sqlgraph.To(competency.Table, competency.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, pillar.CompetenciesTable, pillar.CompetenciesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryScaleCriteria chains the current query on the "scale_criteria" edge.
func (_q *PillarQuery) QueryScaleCriteria() *ScaleCriterionQuery {
	query := (&ScaleCriterionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pillar.Table, pillar.FieldID, selector),
			sqlgraph.To(scalecriterion.Table, scalecriterion.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, pillar.ScaleCriteriaTable, pillar.ScaleCriteriaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Pillar entity from the query.
// Returns a *NotFoundError when no Pillar was found.
func (_q *PillarQuery) First(ctx context.Context) (*Pillar, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pillar.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PillarQuery) FirstX(ctx context.Context) *Pillar {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Pillar ID from the query.
// Returns a *NotFoundError when no Pillar ID was found.
func (_q *PillarQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pillar.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *PillarQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Pillar entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Pillar entity is found.
// Returns a *NotFoundError when no Pillar entities are found.
func (_q *PillarQuery) Only(ctx context.Context) (*Pillar, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pillar.Label}
	default:
		return nil, &NotSingularError{pillar.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PillarQuery) OnlyX(ctx context.Context) *Pillar {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Pillar ID in the query.
// Returns a *NotSingularError when more than one Pillar ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *PillarQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pillar.Label}
	default:
		err = &NotSingularError{pillar.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *PillarQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Pillars.
func (_q *PillarQuery) All(ctx context.Context) ([]*Pillar, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Pillar, *PillarQuery]()
	return withInterceptors[[]*Pillar](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PillarQuery) AllX(ctx context.Context) []*Pillar {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Pillar IDs.
func (_q *PillarQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(pillar.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *PillarQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *PillarQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PillarQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PillarQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PillarQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("internal: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PillarQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PillarQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PillarQuery) Clone() *PillarQuery {
	if _q == nil {
		return nil
	}
	return &PillarQuery{
		config:            _q.config,
		ctx:               _q.ctx.Clone(),
		order:             append([]pillar.OrderOption{}, _q.order...),
		inters:            append([]Interceptor{}, _q.inters...),
		predicates:        append([]predicate.Pillar{}, _q.predicates...),
		withCompetencies:  _q.withCompetencies.Clone(),
		withScaleCriteria: _q.withScaleCriteria.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCompetencies tells the query-builder to eager-load the nodes that are connected to
// the "competencies" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PillarQuery) WithCompetencies(opts ...func(*CompetencyQuery)) *PillarQuery {
	query := (&CompetencyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCompetencies = query
	return _q
}

// WithScaleCriteria tells the query-builder to eager-load the nodes that are connected to
// the "scale_criteria" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PillarQuery) WithScaleCriteria(opts ...func(*ScaleCriterionQuery)) *PillarQuery {
	query := (&ScaleCriterionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withScaleCriteria = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Pillar.Query().
//		GroupBy(pillar.FieldCreatedAt).
//		Aggregate(internal.Count()).
//		Scan(ctx, &v)
func (_q *PillarQuery) GroupBy(field string, fields ...string) *PillarGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PillarGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = pillar.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Pillar.Query().
//		Select(pillar.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *PillarQuery) Select(fields ...string) *PillarSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PillarSelect{PillarQuery: _q}
	sbuild.label = pillar.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PillarSelect configured with the given aggregations.
func (_q *PillarQuery) Aggregate(fns ...AggregateFunc) *PillarSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PillarQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("internal: uninitialized interceptor (forgotten import internal/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !pillar.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("internal: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *PillarQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Pillar, error) {
	var (
		nodes       = []*Pillar{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withCompetencies != nil,
			_q.withScaleCriteria != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Pillar).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Pillar{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCompetencies; query != nil {
		if err := _q.loadCompetencies(ctx, query, nodes,
			func(n *Pillar) { n.Edges.Competencies = []*Competency{} },
			func(n *Pillar, e *Competency) { n.Edges.Competencies = append(n.Edges.Competencies, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withScaleCriteria; query != nil {
		if err := _q.loadScaleCriteria(ctx, query, nodes,
			func(n *Pillar) { n.Edges.ScaleCriteria = []*ScaleCriterion{} },
			func(n *Pillar, e *ScaleCriterion) { n.Edges.ScaleCriteria = append(n.Edges.ScaleCriteria, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *PillarQuery) loadCompetencies(ctx context.Context, query *CompetencyQuery, nodes []*Pillar, init func(*Pillar), assign func(*Pillar, *Competency)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Pillar)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(competency.FieldPillarID)
	}
	query.Where(predicate.Competency(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(pillar.CompetenciesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PillarID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pillar_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PillarQuery) loadScaleCriteria(ctx context.Context, query *ScaleCriterionQuery, nodes []*Pillar, init func(*Pillar), assign func(*Pillar, *ScaleCriterion)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Pillar)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(scalecriterion.FieldPillarID)
	}
	query.Where(predicate.ScaleCriterion(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(pillar.ScaleCriteriaColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PillarID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pillar_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *PillarQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PillarQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pillar.Table, pillar.Columns, sqlgraph.NewFieldSpec(pillar.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pillar.FieldID)
		for i := range fields {
			if fields[i] != pillar.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *PillarQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(pillar.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = pillar.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PillarGroupBy is the group-by builder for Pillar entities.
type PillarGroupBy struct {
	selector
	build *PillarQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PillarGroupBy) Aggregate(fns ...AggregateFunc) *PillarGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PillarGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PillarQuery, *PillarGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *PillarGroupBy) sqlScan(ctx context.Context, root *PillarQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PillarSelect is the builder for selecting fields of Pillar entities.
type PillarSelect struct {
	*PillarQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PillarSelect) Aggregate(fns ...AggregateFunc) *PillarSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PillarSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PillarQuery, *PillarSelect](ctx, _s.PillarQuery, _s, _s.inters, v)
}

func (_s *PillarSelect) sqlScan(ctx context.Context, root *PillarQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
