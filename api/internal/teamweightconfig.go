// Code generated by ent, DO NOT EDIT.

package internal

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/sed-evaluacion-desempeno/api/internal/cycle"
	"github.com/sed-evaluacion-desempeno/api/internal/orgnode"
	"github.com/sed-evaluacion-desempeno/api/internal/teamweightconfig"
)

// TeamWeightConfig is the model entity for the TeamWeightConfig schema.
type TeamWeightConfig struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CycleID holds the value of the "cycle_id" field.
	CycleID uuid.UUID `json:"cycle_id,omitempty"`
	// TeamID holds the value of the "team_id" field.
	TeamID uuid.UUID `json:"team_id,omitempty"`
	// JWeight holds the value of the "j_weight" field.
	JWeight float64 `json:"j_weight,omitempty"`
	// PjWeight holds the value of the "pj_weight" field.
	PjWeight float64 `json:"pj_weight,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TeamWeightConfigQuery when eager-loading is set.
	Edges        TeamWeightConfigEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TeamWeightConfigEdges holds the relations/edges for other nodes in the graph.
type TeamWeightConfigEdges struct {
	// Cycle holds the value of the cycle edge.
	Cycle *Cycle `json:"cycle,omitempty"`
	// TeamNode holds the value of the team_node edge.
	TeamNode *OrgNode `json:"team_node,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CycleOrErr returns the Cycle value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TeamWeightConfigEdges) CycleOrErr() (*Cycle, error) {
	if e.Cycle != nil {
		return e.Cycle, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: cycle.Label}
	}
	return nil, &NotLoadedError{edge: "cycle"}
}

// TeamNodeOrErr returns the TeamNode value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TeamWeightConfigEdges) TeamNodeOrErr() (*OrgNode, error) {
	if e.TeamNode != nil {
		return e.TeamNode, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: orgnode.Label}
	}
	return nil, &NotLoadedError{edge: "team_node"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeamWeightConfig) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case teamweightconfig.FieldJWeight, teamweightconfig.FieldPjWeight:
			values[i] = new(sql.NullFloat64)
		case teamweightconfig.FieldID, teamweightconfig.FieldCycleID, teamweightconfig.FieldTeamID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeamWeightConfig fields.
func (_m *TeamWeightConfig) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teamweightconfig.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case teamweightconfig.FieldCycleID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field cycle_id", values[i])
			} else if value != nil {
				_m.CycleID = *value
			}
		case teamweightconfig.FieldTeamID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field team_id", values[i])
			} else if value != nil {
				_m.TeamID = *value
			}
		case teamweightconfig.FieldJWeight:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field j_weight", values[i])
			} else if value.Valid {
				_m.JWeight = value.Float64
			}
		case teamweightconfig.FieldPjWeight:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field pj_weight", values[i])
			} else if value.Valid {
				_m.PjWeight = value.Float64
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TeamWeightConfig.
// This includes values selected through modifiers, order, etc.
func (_m *TeamWeightConfig) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCycle queries the "cycle" edge of the TeamWeightConfig entity.
func (_m *TeamWeightConfig) QueryCycle() *CycleQuery {
	return NewTeamWeightConfigClient(_m.config).QueryCycle(_m)
}

// QueryTeamNode queries the "team_node" edge of the TeamWeightConfig entity.
func (_m *TeamWeightConfig) QueryTeamNode() *OrgNodeQuery {
	return NewTeamWeightConfigClient(_m.config).QueryTeamNode(_m)
}

// Update returns a builder for updating this TeamWeightConfig.
// Note that you need to call TeamWeightConfig.Unwrap() before calling this method if this TeamWeightConfig
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TeamWeightConfig) Update() *TeamWeightConfigUpdateOne {
	return NewTeamWeightConfigClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TeamWeightConfig entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TeamWeightConfig) Unwrap() *TeamWeightConfig {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("internal: TeamWeightConfig is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TeamWeightConfig) String() string {
	var builder strings.Builder
	builder.WriteString("TeamWeightConfig(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("cycle_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CycleID))
	builder.WriteString(", ")
	builder.WriteString("team_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TeamID))
	builder.WriteString(", ")
	builder.WriteString("j_weight=")
	builder.WriteString(fmt.Sprintf("%v", _m.JWeight))
	builder.WriteString(", ")
	builder.WriteString("pj_weight=")
	builder.WriteString(fmt.Sprintf("%v", _m.PjWeight))
	builder.WriteByte(')')
	return builder.String()
}

// TeamWeightConfigs is a parsable slice of TeamWeightConfig.
type TeamWeightConfigs []*TeamWeightConfig
