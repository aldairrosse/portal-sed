// Code generated by ent, DO NOT EDIT.

package internal

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/sed-evaluacion-desempeno/api/internal/evaluation"
	"github.com/sed-evaluacion-desempeno/api/internal/evaluationgoal"
	"github.com/sed-evaluacion-desempeno/api/internal/goal"
)

// EvaluationGoal is the model entity for the EvaluationGoal schema.
type EvaluationGoal struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// FinalRating holds the value of the "final_rating" field.
	FinalRating *int `json:"final_rating,omitempty"`
	// FinalComments holds the value of the "final_comments" field.
	FinalComments string `json:"final_comments,omitempty"`
	// EvaluationID holds the value of the "evaluation_id" field.
	EvaluationID uuid.UUID `json:"evaluation_id,omitempty"`
	// GoalID holds the value of the "goal_id" field.
	GoalID uuid.UUID `json:"goal_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EvaluationGoalQuery when eager-loading is set.
	Edges        EvaluationGoalEdges `json:"edges"`
	selectValues sql.SelectValues
}

// EvaluationGoalEdges holds the relations/edges for other nodes in the graph.
type EvaluationGoalEdges struct {
	// Evaluation holds the value of the evaluation edge.
	Evaluation *Evaluation `json:"evaluation,omitempty"`
	// Goal holds the value of the goal edge.
	Goal *Goal `json:"goal,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// EvaluationOrErr returns the Evaluation value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EvaluationGoalEdges) EvaluationOrErr() (*Evaluation, error) {
	if e.Evaluation != nil {
		return e.Evaluation, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: evaluation.Label}
	}
	return nil, &NotLoadedError{edge: "evaluation"}
}

// GoalOrErr returns the Goal value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EvaluationGoalEdges) GoalOrErr() (*Goal, error) {
	if e.Goal != nil {
		return e.Goal, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: goal.Label}
	}
	return nil, &NotLoadedError{edge: "goal"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EvaluationGoal) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case evaluationgoal.FieldFinalRating:
			values[i] = new(sql.NullInt64)
		case evaluationgoal.FieldFinalComments:
			values[i] = new(sql.NullString)
		case evaluationgoal.FieldCreatedAt, evaluationgoal.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case evaluationgoal.FieldID, evaluationgoal.FieldEvaluationID, evaluationgoal.FieldGoalID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EvaluationGoal fields.
func (_m *EvaluationGoal) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case evaluationgoal.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case evaluationgoal.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case evaluationgoal.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case evaluationgoal.FieldFinalRating:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field final_rating", values[i])
			} else if value.Valid {
				_m.FinalRating = new(int)
				*_m.FinalRating = int(value.Int64)
			}
		case evaluationgoal.FieldFinalComments:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field final_comments", values[i])
			} else if value.Valid {
				_m.FinalComments = value.String
			}
		case evaluationgoal.FieldEvaluationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field evaluation_id", values[i])
			} else if value != nil {
				_m.EvaluationID = *value
			}
		case evaluationgoal.FieldGoalID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field goal_id", values[i])
			} else if value != nil {
				_m.GoalID = *value
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EvaluationGoal.
// This includes values selected through modifiers, order, etc.
func (_m *EvaluationGoal) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryEvaluation queries the "evaluation" edge of the EvaluationGoal entity.
func (_m *EvaluationGoal) QueryEvaluation() *EvaluationQuery {
	return NewEvaluationGoalClient(_m.config).QueryEvaluation(_m)
}

// QueryGoal queries the "goal" edge of the EvaluationGoal entity.
func (_m *EvaluationGoal) QueryGoal() *GoalQuery {
	return NewEvaluationGoalClient(_m.config).QueryGoal(_m)
}

// Update returns a builder for updating this EvaluationGoal.
// Note that you need to call EvaluationGoal.Unwrap() before calling this method if this EvaluationGoal
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EvaluationGoal) Update() *EvaluationGoalUpdateOne {
	return NewEvaluationGoalClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EvaluationGoal entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EvaluationGoal) Unwrap() *EvaluationGoal {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("internal: EvaluationGoal is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EvaluationGoal) String() string {
	var builder strings.Builder
	builder.WriteString("EvaluationGoal(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.FinalRating; v != nil {
		builder.WriteString("final_rating=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("final_comments=")
	builder.WriteString(_m.FinalComments)
	builder.WriteString(", ")
	builder.WriteString("evaluation_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EvaluationID))
	builder.WriteString(", ")
	builder.WriteString("goal_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.GoalID))
	builder.WriteByte(')')
	return builder.String()
}

// EvaluationGoals is a parsable slice of EvaluationGoal.
type EvaluationGoals []*EvaluationGoal
