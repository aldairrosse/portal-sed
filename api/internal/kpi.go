// Code generated by ent, DO NOT EDIT.

package internal

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/sed-evaluacion-desempeno/api/internal/kpi"
)

// KPI is the model entity for the KPI schema.
type KPI struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Unit holds the value of the "unit" field.
	Unit kpi.Unit `json:"unit,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the KPIQuery when eager-loading is set.
	Edges        KPIEdges `json:"edges"`
	selectValues sql.SelectValues
}

// KPIEdges holds the relations/edges for other nodes in the graph.
type KPIEdges struct {
	// GoalLinks holds the value of the goal_links edge.
	GoalLinks []*GoalKpiLink `json:"goal_links,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// GoalLinksOrErr returns the GoalLinks value or an error if the edge
// was not loaded in eager-loading.
func (e KPIEdges) GoalLinksOrErr() ([]*GoalKpiLink, error) {
	if e.loadedTypes[0] {
		return e.GoalLinks, nil
	}
	return nil, &NotLoadedError{edge: "goal_links"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*KPI) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case kpi.FieldName, kpi.FieldUnit, kpi.FieldDescription:
			values[i] = new(sql.NullString)
		case kpi.FieldCreatedAt, kpi.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case kpi.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the KPI fields.
func (_m *KPI) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case kpi.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case kpi.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case kpi.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case kpi.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case kpi.FieldUnit:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field unit", values[i])
			} else if value.Valid {
				_m.Unit = kpi.Unit(value.String)
			}
		case kpi.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the KPI.
// This includes values selected through modifiers, order, etc.
func (_m *KPI) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGoalLinks queries the "goal_links" edge of the KPI entity.
func (_m *KPI) QueryGoalLinks() *GoalKpiLinkQuery {
	return NewKPIClient(_m.config).QueryGoalLinks(_m)
}

// Update returns a builder for updating this KPI.
// Note that you need to call KPI.Unwrap() before calling this method if this KPI
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *KPI) Update() *KPIUpdateOne {
	return NewKPIClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the KPI entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *KPI) Unwrap() *KPI {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("internal: KPI is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *KPI) String() string {
	var builder strings.Builder
	builder.WriteString("KPI(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("unit=")
	builder.WriteString(fmt.Sprintf("%v", _m.Unit))
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteByte(')')
	return builder.String()
}

// KPIs is a parsable slice of KPI.
type KPIs []*KPI
