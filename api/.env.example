# Minimal env for running api outside compose (see root .env.example for full list).
DATABASE_URL=postgres://sed:sed@localhost:5432/sed?sslmode=disable
MAIL_PROVIDER=no-op
SENDGRID_API_KEY=
MAIL_FROM=no-reply@sed.local
APP_BASE_URL=http://localhost:5173
