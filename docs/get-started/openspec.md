# OpenSpec en SED

> Gate: usar solo si existe `openspec/` o pedido explícito; si usuario niega openspec/opsx → prohibición total. Flujo delegado a comando global `~/.config/opencode/commands/openspec.md` (lazy).

```bash
rtk proxy openspec validate --all
rtk proxy openspec list --specs
rtk proxy openspec status
```
