services:
  web:
    build:
      context: ./web
      args:
        VITE_SHOW_DEV_TOOLBAR: ${VITE_SHOW_DEV_TOOLBAR:-false}
    ports:
      - "${WEB_PORT:-8080}:80"
    depends_on:
      api:
        condition: service_started

  api:
    build:
      context: ./web
      dockerfile: Dockerfile.api
    volumes:
      - ./data:/data
